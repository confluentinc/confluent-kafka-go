name: confluent-kafka-go
lang: go
lang_version: 1.21
git:
  enable: true
github:
  enable: true
  repo_name: confluentinc/confluent-kafka-go
semaphore:
  enable: true
sonarqube:
  enable: true
  coverage_reports:
    - "**/*coverage.txt"
